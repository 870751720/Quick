import test from'node:test';import assert from'node:assert/strict';import{damage,RULES}from'../src/combat.js';test('普通攻击造成固定伤害',()=>{let a={hp:100};assert.equal(damage(a,RULES.attackDamage),true);assert.equal(a.hp,72)});test('受击保护期间免伤',()=>{let a={hp:20};assert.equal(damage(a,10,true),false);assert.equal(a.hp,20)});test('伤害不会令生命成为负数',()=>{let a={hp:5};damage(a,18);assert.equal(a.hp,0)});
