import test from'node:test';import assert from'node:assert/strict';import{spend,damage,regen}from'../src/combat.js';test('精力不可透支',()=>{let a={stamina:20};assert.equal(spend(a,24),false);assert.equal(a.stamina,20)});test('无敌帧免伤',()=>{let a={hp:20};assert.equal(damage(a,10,true),false);assert.equal(a.hp,20)});test('恢复不超上限',()=>{let a={stamina:90,maxStamina:100};regen(a,1);assert.equal(a.stamina,100)});
